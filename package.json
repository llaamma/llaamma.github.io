{
  "name": "web-game-slot-machine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add dist && git commit -m \"deploy\" && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
